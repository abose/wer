name: Publish extension to phcode.dev extension store test2

on:
  release:
    types:
      - edited
      - published
      - released

jobs:
  build-tasks:
    runs-on: ubuntu-latest
    steps:
      - name: Dump GitHub context
        id: github_context_step
        run: echo '${{ toJSON(github) }}'
      - name: release details
        run: |
          echo "$GITHUB_REF repo=${{github.event.repository.full_name}}"
          curl http://localhost:5000/publishGithubRelease?releaseRef=${{github.event.repository.full_name}}:$GITHUB_REF
